export type AuthState = {
  isSignedIn: boolean;
  isLoading: boolean;
  id: string | undefined;
  name: string | undefined;
};

export type MyWorhip = {
  worship: string;
};

export type MyToken = {
  token: string;
};
